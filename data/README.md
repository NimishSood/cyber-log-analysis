# Data (local only)

This project expects large raw datasets to live under `data/`, but those files are intentionally **not** tracked in Git (GitHub rejects very large files).

Place your local datasets in this folder (for example `data/raw/`).

